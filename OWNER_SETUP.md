# راه‌اندازی پنل مالک و شبکه مقصدها

## فقط یک Secret

برای اجرای پروژه فقط این Secret لازم است:

- `TELEGRAM_BOT_TOKEN` — توکن BotFather ربات `@Climavid_bot`

شناسه مقصد و شناسه مالک به‌صورت Secret جداگانه لازم نیست. شبکه مقصدهای خصوصی و شناسه مالک داخل `data/private_state.enc` و با کلیدی مشتق‌شده از Bot Token رمزنگاری می‌شوند.

## اولین ورود مالک

در اولین راه‌اندازی، مالک باید در گفت‌وگوی خصوصی Bot این فرمان را ارسال کند:

`/claim`

این چت به‌عنوان پنل مالک ثبت می‌شود. سپس دستورات مدیریتی فقط در همان چت خصوصی پاسخ داده می‌شوند.

> نکته: چون Telegram Bot API هویت مالک Bot Token را در پیام‌های عادی اعلام نمی‌کند، بهتر است مالک اولین فردی باشد که پس از راه‌اندازی `/claim` را می‌فرستد.

## دستورات مالک

- `/help` — راهنمای پنل مالک
- `/status` — وضعیت کلی سیستم
- `/report` — گزارش کامل و آمار شبکه
- `/network` — تعداد و نام گروه‌ها و کانال‌های فعال
- `/logs` — لاگ فنی و خطاهای اخیر
- `/health` — بررسی سلامت Bot و مقصدهای ثبت‌شده

هیچ کاربر عادی به تعداد، نام یا تنظیمات سایر مقصدها دسترسی ندارد.

## اضافه شدن گروه یا کانال جدید

هر زمان Bot به یک گروه یا کانال اضافه و Administrator شود، Telegram رویداد `my_chat_member` ارسال می‌کند. سیستم مقصد را به‌صورت خودکار ثبت می‌کند و تنظیمات پیش‌فرض زیر را می‌دهد:

- وضعیت: فعال
- تعداد: ۱ پست در روز
- زمان: ۲۰:۰۰ به وقت تهران

برای گروه/سوپرگروه، مدیر می‌تواند داخل همان گروه تنظیمات را تغییر دهد.

## دستورات مدیر مقصد

- `/help` — راهنمای فارسی
- `/setup` — تنظیمات فعلی همان مقصد
- `/posts 1` — یک پست در روز
- `/posts 2` — دو پست در روز
- `/posts 3` — سه پست در روز
- `/times 20:00` — ساعت یک پست
- `/times 10:00 20:00` — ساعت دو پست
- `/times 10:00 20:00 22:00` — ساعت سه پست
- `/on` — فعال‌سازی
- `/off` — توقف موقت

تغییرات هر مقصد فقط روی همان مقصد اعمال می‌شود.

## توزیع محتوا

محتوای انتخاب‌شده ClimaVids برای مقصدهای فعال مشترک است. هر مقصد آن محتوا را طبق برنامه خودش دریافت می‌کند. سیستم برای جلوگیری از اسپم، اگر چند زمان از دست رفته باشد در هر اجرای Workflow حداکثر یک ارسال جبرانی برای هر مقصد انجام می‌دهد.

## گزارش‌های خودکار

در صورت ثبت مالک:

- ساعت 09:00 تهران: گزارش صبحگاهی
- ساعت 21:00 تهران: گزارش شبانه
- در صورت خطای انتشار یا مانیتورینگ: هشدار خصوصی برای مالک

اطلاعات خصوصی شبکه به‌صورت ciphertext در Repository نگهداری می‌شود و plaintext آن وارد GitHub نمی‌شود.
