# راه‌اندازی پنل خصوصی مالک

## Secrets

سه Secret لازم است:

- `TELEGRAM_BOT_TOKEN` — توکن BotFather
- `TELEGRAM_CHAT_ID` — شناسه عددی مقصد انتشار
- `TELEGRAM_OWNER_CHAT_ID` — شناسه عددی چت خصوصی مالک با ربات

تا زمانی که `TELEGRAM_OWNER_CHAT_ID` تنظیم نشده باشد، سیستم انتشار محتوا فعال می‌ماند اما Owner Monitor و گزارش‌های خصوصی خاموش هستند.

## پیدا کردن TELEGRAM_OWNER_CHAT_ID

1. در Telegram به ربات خود پیام `/start` یا `/help` بفرستید.
2. شناسه عددی چت خصوصی خود را از یک سرویس/ربات مطمئن برای نمایش Chat ID دریافت کنید.
3. مقدار عددی را بدون @ در GitHub Secrets قرار دهید.

## دستورات خصوصی مالک

- `/help`
- `/status`
- `/report`
- `/logs`
- `/test`

این دستورات فقط وقتی پاسخ داده می‌شوند که Chat ID فرستنده دقیقاً برابر `TELEGRAM_OWNER_CHAT_ID` باشد.

## زمان گزارش

Owner Monitor هر 15 دقیقه اجرا می‌شود و در صورت وجود Secret مالک:

- ساعت 09:00 تهران: گزارش صبحگاهی
- ساعت 21:00 تهران: گزارش شبانه
- هنگام خطای Workflow: هشدار خطا در چت خصوصی مالک

گزارش‌ها برای جلوگیری از تکرار با `data/owner_state.json` کنترل می‌شوند.

## تست مقصد انتشار

پس از تنظیم Secrets، Workflow مربوط به Owner Monitor را دستی اجرا کنید یا `/test` را در چت خصوصی ربات بفرستید. تست مقصد چیزی منتشر نمی‌کند و وضعیت عضویت/دسترسی Bot را گزارش می‌دهد.
