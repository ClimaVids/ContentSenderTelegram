# مالک ربات

این پروژه فقط به `TELEGRAM_BOT_TOKEN` نیاز دارد.

## راه‌اندازی مالک

1. Token ربات `@Climavid_bot` را در GitHub Secret با نام `TELEGRAM_BOT_TOKEN` قرار دهید.
2. ربات `@Climavid_bot` را به کانال `@climavids` اضافه کنید و برای آن دسترسی Administrator و اجازه ارسال پیام بدهید.
3. در گفت‌وگوی خصوصی با ربات فرمان `/claim` را بفرستید.
4. سیستم شناسه همان گفت‌وگو را در `data/owner_state.json` ذخیره می‌کند.
5. سپس `/test` را بفرستید تا Bot API، مقصد و دسترسی ربات بررسی شود.

## فرمان‌ها

`/claim` ثبت پنل مالک

`/help` راهنمای کامل

`/status` وضعیت فعلی

`/report` گزارش کامل

`/logs` جزئیات لاگ و خطا

`/test` بررسی اتصال و دسترسی بدون انتشار

## گزارش خودکار

سیستم هر روز دو گزارش برای مالک ارسال می‌کند، حدود ساعت 09:00 و 21:00 تهران. در صورت خطای Workflow نیز هشدار خصوصی ارسال می‌شود.

## نکته امنیتی

در این معماری هیچ Chat ID یا Owner ID به‌عنوان Secret ذخیره نمی‌شود. مالکیت داخل state پروژه ثبت می‌شود و فقط یک‌بار از طریق `/claim` تعیین می‌گردد.
