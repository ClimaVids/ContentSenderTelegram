# دستورات پنل مالک

`/claim` — اولین ثبت مالک در چت خصوصی

`/help` — راهنمای داخلی ربات

`/status` — وضعیت کلی سیستم

`/report` — گزارش کامل

`/logs` — لاگ‌ها و خطاهای اخیر

`/test` — بررسی Bot API، مقصد `@climavids` و دسترسی ربات بدون ارسال پست
